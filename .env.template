# Nombre del escáner en el sistema (por ejemplo, parte del nombre como 'RICOH' o el driver WIA específico)
SCANNER_NAME=RICOH IM C300 [58387958FB4C]


# Directorio de salida de los documentos escaneados
SCAN_OUTPUT_DIR=scanned_documents

# Resolución en DPI para el escaneo (ej. 150, 200, 300)
SCAN_DPI=200

# Formato de color: 1 = Color, 2 = Grayscale, 4 = Black & White
SCAN_COLOR_MODE=1

# Formato de archivo de imagen resultante: JPG, PNG o TIFF
SCAN_FORMAT=JPG

# Origen del papel: ADF o FLATBED
SCAN_SOURCE=ADF

# Delays de hardware (segundos) para sincronizar con el alimentador físico y el sistema operativo
# tiempo de espera en segundo para la succion de papeles de la bandeja de escaneo masivo (si da error de red, aumentar este valor)
SCAN_ADF_DELAY=2.5
# Tiempo en segundos para esperar que el sistema termine de transformar el .bmp crudo en .jpg
SCAN_FILE_WRITE_DELAY=0.5

# Clave API de Google Gemini (Obtener en https://aistudio.google.com/)
GEMINI_API_KEY=tu_clave_api_aqui

# Proveedor de Inteligencia Artificial: gemini (en la nube), local (Ollama/vLLM local) o powerautomate (flujo en Power Automate)
AI_PROVIDER=gemini

# Configuración del VLM Local (Ollama / vLLM)
# Para Ollama local usar base_url http://localhost:11434/v1 y api_key 'ollama'
# Para vLLM o proxy en producción usar la URL y token respectivos
VLM_API_URL=http://localhost:11434/v1
VLM_API_KEY=ollama
VLM_MODEL=qwen2.5vl-large-ctx

# Tamaño de contexto del modelo de visión (Qwen2.5-VL requiere al menos 8192 o 16384 para procesar imágenes)
VLM_NUM_CTX=16384

# Configuración para el modo de servicio Power Automate y SharePoint
# POWERAUTOMATE_URL=https://<tenant_fqdn>/powerautomate/automations/direct/cu/26/workflows/...
# SHAREPOINT_FQDN=https://<tenant_name>.sharepoint.com/:i:/r/sites/<sitio>

# Configuración de base de datos MySQL en AWS
# DB_HOST=dydb2-instance-1.cz8kik28igwg.us-east-1.rds.amazonaws.com
# DB_PORT=3306
# DB_NAME=Firma_de_remitos
# DB_USER=tu_usuario_db
# DB_PASSWORD=tu_contraseña_db

# Configuración de integración con API de Finnegans
# FINNEGANS_CLIENT_ID=tu_client_id_aqui
# FINNEGANS_CLIENT_SECRET=tu_client_secret_aqui
# FINNEGANS_EMPRESA_COD=EMPRE01
# FINNEGANS_HTTP_TIMEOUT=90
# SYNC_DAYS_BACK=7
# FINNEGANS_TOKEN_URL=https://api.teamplace.finneg.com/api/oauth/token
# FINNEGANS_API_URL=https://api.teamplace.finneg.com/api/reports
# FINNEGANS_REMITOS_REPORT_NAME=AFIRMAREMVEN_MG




